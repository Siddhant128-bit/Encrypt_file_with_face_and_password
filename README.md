# Encrypt_file_with_face_and_password
Run FaceLocker.py 
Select Encrypt/type address of the normal file, password to lock it with, type the name of the picture so it can run face recognition in verification process
Select Decrypt/ type the address of encrypted file, password used to lock it, type the name of the picture you had said as for your face 
